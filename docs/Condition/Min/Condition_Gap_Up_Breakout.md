# 조건식 (Condition)

- STOM 주식 자동거래에 사용하기 위한 조건식 문서
- [[Back_Testing_Guideline_Min]] 을(를) 기반으로 작성한 Min 조건식
- [[Condition_Document_Template_Guideline]] 을(를) 바탕으로 템플릿 구조를 적용한 문서

## 목차
- [조건식 (Condition)](#조건식-condition)
  - [목차](#목차)
  - [소개](#소개)
  - [가이드라인](#가이드라인)
    - [저장 이름 규칙](#저장-이름-규칙)
- [Condition - Gap Up Breakout](#condition---gap-up-breakout)
  - [조건식](#조건식)
    - [매수 조건식](#매수-조건식)
    - [매도 조건식](#매도-조건식)

## 개요

본 문서는 STOM 주식 자동거래 시스템에서 **장 초반(09:00~10:00) 갭상승 돌파 전략**을 정의한다.

- **대상 시간 구간**: 09:00:00 ~ 10:00:00 (장 시작 1시간)
- **대상 종목**: 가격대 2,000원~50,000원, 등락율 2~20%
- **전략 타입**: 갭상승 + 고가 돌파 포착 (Gap Up Breakout)
- **핵심 변수**: 시가갭상승율, 최고현재가(5), MACD, RSI, 체결강도
- **업데이트 이력**:
  - 초기 문서 작성

## 소개

이 문서는 장초 09:00~10:00 구간에서 갭상승 이후 돌파 패턴을 포착하는 분봉 전략입니다.
시가 대비 2% 이상 갭상승한 종목이 고가를 돌파할 때 추가 상승을 기대하고 진입합니다.

## 가이드라인

- 모든 조건식은 Python으로 작성되어 있으며, 주식 거래의 자동화를 목표로 합니다.
- 각 조건식은 특정 조건을 만족할 때 매수 또는 매도를 실행합니다.
- 분봉 데이터 기반으로 900~1518 (09:00~15:18) 시간에만 거래합니다.

### 저장 이름 규칙

1. **기본 규칙**:
   - `C`: Condition을 의미합니다.
   - `GUB`: Gap Up Breakout을 의미합니다.
   - `B`: 매수 조건식을 의미합니다.
   - `S`: 매도 조건식을 의미합니다.
   - `M`: Min (분봉) 조건식을 의미합니다.

2. **예시**:
   - `C_GUB_M_B`: Gap Up Breakout 분봉 매수 조건식
   - `C_GUB_M_S`: Gap Up Breakout 분봉 매도 조건식

---

# Condition - Gap Up Breakout

## 조건식

### 매수 조건식

```python
# 장초 갭상승 돌파 전략 (09:00 ~ 10:00)
전일종가 = 현재가 / (1 + (등락율 / 100))
시가갭상승율 = ((시가 - 전일종가) / 전일종가) * 100

# 시간 조건 (09:00 ~ 10:00)
if not (90000 <= 시분초 <= 100000):
    매수 = False

# 데이터 충분성 확인
elif not (데이터길이 > 5):
    매수 = False

# 가격 및 등락율 조건
elif not (2000 < 현재가 <= 50000):
    매수 = False
elif not (2.0 < 등락율 <= 20.0):
    매수 = False

# VI 제외
elif not (현재가 < VI아래5호가):
    매수 = False

# 갭상승 조건 (핵심)
elif not (시가갭상승율 >= 2.0):
    매수 = False

# 고가 돌파 조건
elif not (현재가 > 고가N(1)):
    매수 = False
elif not (분봉고가 > 분봉고가N(1)):
    매수 = False

# 분봉 양봉 확인
elif not (현재가 > 분봉시가):
    매수 = False

# 거래량 조건
elif not (분당거래대금 > 분당거래대금평균(10) * 1.5):
    매수 = False
elif not (분당거래대금 > 분당거래대금N(1)):
    매수 = False

# 체결강도 조건
elif not (체결강도 > 120):
    매수 = False
elif not (체결강도 > 체결강도N(1)):
    매수 = False

# 전일비 및 전일동시간비 조건
elif not (전일비 > 100):
    매수 = False
elif not (전일동시간비 > 150):
    매수 = False

# 시가총액별 세분화 조건
elif 시가총액 < 2000:
    # 소형주: 강한 모멘텀 요구
    if not (등락율각도(10) > 12):
        매수 = False
    elif not (당일거래대금 > 50 * 100):
        매수 = False
    elif not (회전율 > 3):
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * 1.5):
        매수 = False
    elif not (현재가 > 고가 - (고가 - 저가) * 15 / 100):
        매수 = False
    elif not ((현재가 / 저가 - 1) * 100 <= 12):
        매수 = False

elif 시가총액 < 5000:
    # 중형주
    if not (등락율각도(10) > 10):
        매수 = False
    elif not (당일거래대금 > 100 * 100):
        매수 = False
    elif not (회전율 > 2):
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * 1.3):
        매수 = False
    elif not (현재가 > 고가 - (고가 - 저가) * 20 / 100):
        매수 = False
    elif not ((현재가 / 저가 - 1) * 100 <= 10):
        매수 = False

else:
    # 대형주
    if not (등락율각도(10) > 8):
        매수 = False
    elif not (당일거래대금 > 200 * 100):
        매수 = False
    elif not (회전율 > 1.5):
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * 1.2):
        매수 = False
    elif not (현재가 > 고가 - (고가 - 저가) * 25 / 100):
        매수 = False
    elif not ((현재가 / 저가 - 1) * 100 <= 8):
        매수 = False

# 최종 매수 실행
if 매수:
    self.Buy(종목코드, 종목명, 매수수량, 현재가, 매도호가1, 매수호가1, 데이터길이)
```

### 매도 조건식

```python
# 돌파 실패 및 익절 전략
if 등락율 > 29.5:
    매도 = True
elif 수익률 >= 4.0:
    매도 = True
elif 수익률 <= -3.0:
    매도 = True
elif 최고수익률 - 수익률 >= 3.0:
    매도 = True
elif 보유시간 > 120:  # 120분 (2시간) 초과 보유 시 청산
    매도 = True

# 고가 대비 하락 조건
elif 현재가 < 고가 * 0.97:
    if 수익률 > 0.5:
        매도 = True

# 분봉 음봉 전환
elif 현재가 < 분봉시가:
    if 수익률 > 1.0:
        매도 = True

# 체결강도 약화
elif 체결강도 < 100:
    매도 = True

# 거래량 급감
elif 분당거래대금 < 분당거래대금평균(10) * 0.6:
    if 수익률 > 0:
        매도 = True

# 시가총액별 세분화 매도 조건
elif 시가총액 < 2000:
    # 소형주: 빠른 청산
    if 1.5 <= 수익률 < 4.0 and 분당매도수량 > 분당매수수량 * 1.8:
        매도 = True
    elif 등락율각도(10) < 5:
        매도 = True

elif 시가총액 < 5000:
    # 중형주
    if 1.5 <= 수익률 < 4.0 and 분당매도수량 > 분당매수수량 * 1.6:
        매도 = True
    elif 등락율각도(10) < 6:
        매도 = True

else:
    # 대형주
    if 1.5 <= 수익률 < 4.0 and 분당매도수량 > 분당매수수량 * 1.5:
        매도 = True
    elif 등락율각도(10) < 7:
        매도 = True

if 매도:
    self.Sell(종목코드, 종목명, 매도수량, 현재가, 매도호가1, 매수호가1, 강제청산)
```

---

**Last Update: 2025-01-18**

## 최적화 조건식

### 매수 최적화 조건식 - C_GUB_M_BO

```python
# 장초 갭상승 돌파 전략 (09:00 ~ 10:00)
전일종가 = 현재가 / (1 + (등락율 / 100))
시가갭상승율 = ((시가 - 전일종가) / 전일종가) * 100

# 시간 조건 (09:00 ~ 10:00)
if not (90000 <= 시분초 <= 100000):
    매수 = False

# 데이터 충분성 확인
elif not (데이터길이 > 5):
    매수 = False

# 가격 및 등락율 조건
elif not (self.vars[0] < 현재가 <= self.vars[1]):                             # 현재가 범위 최적화
    매수 = False
elif not (self.vars[2] < 등락율 <= self.vars[3]):                              # 등락율 범위 최적화
    매수 = False

# VI 제외
elif not (현재가 < VI아래5호가):
    매수 = False

# 갭상승 조건
elif not (시가갭상승율 >= self.vars[4]):                                       # 갭상승율 하한 최적화
    매수 = False

# 고가 돌파 조건
elif not (현재가 > 고가N(1)):
    매수 = False
elif not (분봉고가 > 분봉고가N(1)):
    매수 = False

# 분봉 양봉 확인
elif not (현재가 > 분봉시가):
    매수 = False

# 거래량 조건
elif not (분당거래대금 > 분당거래대금평균(10) * self.vars[5]):                 # 분당거래대금 비율 최적화
    매수 = False
elif not (분당거래대금 > 분당거래대금N(1)):
    매수 = False

# 체결강도 조건
elif not (체결강도 > self.vars[6]):                                            # 체결강도 하한 최적화
    매수 = False
elif not (체결강도 > 체결강도N(1)):
    매수 = False

# 전일비 및 전일동시간비 조건
elif not (전일비 > self.vars[7]):                                              # 전일비 하한 최적화
    매수 = False
elif not (전일동시간비 > self.vars[8]):                                        # 전일동시간비 하한 최적화
    매수 = False

# 시가총액별 세분화 조건
elif 시가총액 < self.vars[9]:                                                  # 시가총액 기준1 최적화
    # 소형주: 강한 모멘텀 요구
    if not (등락율각도(10) > self.vars[10]):                                   # 등락율각도 하한 최적화 - 소형
        매수 = False
    elif not (당일거래대금 > self.vars[11] * 100):                             # 당일거래대금 하한 최적화 - 소형
        매수 = False
    elif not (회전율 > self.vars[12]):                                         # 회전율 하한 최적화 - 소형
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * self.vars[13]):                     # 매수매도 비율 최적화 - 소형
        매수 = False
    elif not (현재가 > 고가 - (고가 - 저가) * self.vars[14] / 100):           # 고가 근처 비율 최적화 - 소형
        매수 = False

elif 시가총액 < self.vars[15]:                                                 # 시가총액 기준2 최적화
    # 중형주
    if not (등락율각도(10) > self.vars[16]):                                   # 등락율각도 하한 최적화 - 중형
        매수 = False
    elif not (당일거래대금 > self.vars[17] * 100):                             # 당일거래대금 하한 최적화 - 중형
        매수 = False
    elif not (회전율 > self.vars[18]):                                         # 회전율 하한 최적화 - 중형
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * self.vars[19]):                     # 매수매도 비율 최적화 - 중형
        매수 = False

else:
    # 대형주
    if not (등락율각도(10) > self.vars[20]):                                   # 등락율각도 하한 최적화 - 대형
        매수 = False
    elif not (당일거래대금 > self.vars[21] * 100):                             # 당일거래대금 하한 최적화 - 대형
        매수 = False
    elif not (회전율 > self.vars[22]):                                         # 회전율 하한 최적화 - 대형
        매수 = False
    elif not (분당매수수량 > 분당매도수량 * self.vars[23]):                     # 매수매도 비율 최적화 - 대형
        매수 = False
```

### 매수 최적화 범위 - C_GUB_M_BOR

```python
self.vars[0]  = [[1500, 2500, 500], 2000]              # 범위갯수: 3   # 현재가 하한 (원)
self.vars[1]  = [[45000, 55000, 5000], 50000]          # 범위갯수: 3   # 현재가 상한 (원)
self.vars[2]  = [[1.5, 2.5, 0.5], 2.0]                 # 범위갯수: 3   # 등락율 하한 (%)
self.vars[3]  = [[18, 22, 2], 20]                      # 범위갯수: 3   # 등락율 상한 (%)
self.vars[4]  = [[1.5, 2.5, 0.5], 2.0]                 # 범위갯수: 3   # 갭상승율 하한 (%)
self.vars[5]  = [[1.3, 1.7, 0.2], 1.5]                 # 범위갯수: 3   # 분당거래대금 비율
self.vars[6]  = [[115, 125, 5], 120]                   # 범위갯수: 3   # 체결강도 하한
self.vars[7]  = [[90, 110, 10], 100]                   # 범위갯수: 3   # 전일비 하한 (%)
self.vars[8]  = [[135, 165, 15], 150]                  # 범위갯수: 3   # 전일동시간비 하한 (%)
self.vars[9]  = [[1800, 2200, 200], 2000]              # 범위갯수: 3   # 시가총액 기준1 (억원) - 소형
self.vars[10] = [[10, 14, 2], 12]                      # 범위갯수: 3   # 등락율각도 하한 (도) - 소형
self.vars[11] = [[45, 55, 5], 50]                      # 범위갯수: 3   # 당일거래대금 하한 (억원) - 소형
self.vars[12] = [[2.5, 3.5, 0.5], 3.0]                 # 범위갯수: 3   # 회전율 하한 (%) - 소형
self.vars[13] = [[1.3, 1.7, 0.2], 1.5]                 # 범위갯수: 3   # 매수매도 비율 - 소형
self.vars[14] = [[13, 17, 2], 15]                      # 범위갯수: 3   # 고가 근처 비율 (%) - 소형
self.vars[15] = [[4500, 5500, 500], 5000]              # 범위갯수: 3   # 시가총액 기준2 (억원) - 중형
self.vars[16] = [[8, 12, 2], 10]                       # 범위갯수: 3   # 등락율각도 하한 (도) - 중형
self.vars[17] = [[90, 110, 10], 100]                   # 범위갯수: 3   # 당일거래대금 하한 (억원) - 중형
self.vars[18] = [[1.5, 2.5, 0.5], 2.0]                 # 범위갯수: 3   # 회전율 하한 (%) - 중형
self.vars[19] = [[1.2, 1.4, 0.1], 1.3]                 # 범위갯수: 3   # 매수매도 비율 - 중형
self.vars[20] = [[6, 10, 2], 8]                        # 범위갯수: 3   # 등락율각도 하한 (도) - 대형
self.vars[21] = [[180, 220, 20], 200]                  # 범위갯수: 3   # 당일거래대금 하한 (억원) - 대형
self.vars[22] = [[1.2, 1.8, 0.3], 1.5]                 # 범위갯수: 3   # 회전율 하한 (%) - 대형
self.vars[23] = [[1.1, 1.3, 0.1], 1.2]                 # 범위갯수: 3   # 매수매도 비율 - 대형

# 총 경우의 수: 3^24 = 282,429,536,481 (GA 최적화 필수)
```

### 매도 최적화 조건식 - C_GUB_M_SO

```python
if 등락율 > 29.5:
    매도 = True
elif 수익률 >= self.vars[0]:                                                   # 목표 수익률 최적화
    매도 = True
elif 수익률 <= self.vars[1]:                                                   # 손절 수익률 최적화
    매도 = True
elif 최고수익률 - 수익률 >= self.vars[2]:                                      # 고점 대비 하락폭 최적화
    매도 = True
elif 보유시간 > self.vars[3]:                                                  # 최대 보유시간 최적화
    매도 = True

# 고가 대비 하락 조건
elif 현재가 < 고가 * self.vars[4]:                                             # 고가 대비 비율 최적화
    if 수익률 > self.vars[5]:                                                  # 고가 하락 시 수익률 최적화
        매도 = True

# 분봉 음봉 전환
elif 현재가 < 분봉시가:
    if 수익률 > self.vars[6]:                                                  # 음봉 전환 시 수익률 최적화
        매도 = True

# 체결강도 약화
elif 체결강도 < self.vars[7]:                                                  # 체결강도 기준 최적화
    매도 = True

# 거래량 급감
elif 분당거래대금 < 분당거래대금평균(10) * self.vars[8]:                        # 거래대금 비율 최적화
    if 수익률 > 0:
        매도 = True

# 시가총액별 세분화 매도 조건
elif 시가총액 < self.vars[9]:                                                  # 시가총액 기준 최적화 - 소형
    # 소형주: 빠른 청산
    if self.vars[10] <= 수익률 < self.vars[11] and 분당매도수량 > 분당매수수량 * self.vars[12]:  # 수익률 범위 및 매도량 비율 최적화 - 소형
        매도 = True
    elif 등락율각도(10) < self.vars[13]:                                       # 등락율각도 기준 최적화 - 소형
        매도 = True

elif 시가총액 < self.vars[14]:                                                 # 시가총액 기준 최적화 - 중형
    # 중형주
    if self.vars[15] <= 수익률 < self.vars[16] and 분당매도수량 > 분당매수수량 * self.vars[17]:  # 수익률 범위 및 매도량 비율 최적화 - 중형
        매도 = True
    elif 등락율각도(10) < self.vars[18]:                                       # 등락율각도 기준 최적화 - 중형
        매도 = True

else:
    # 대형주
    if self.vars[19] <= 수익률 < self.vars[20] and 분당매도수량 > 분당매수수량 * self.vars[21]:  # 수익률 범위 및 매도량 비율 최적화 - 대형
        매도 = True
    elif 등락율각도(10) < self.vars[22]:                                       # 등락율각도 기준 최적화 - 대형
        매도 = True
```

### 매도 최적화 범위 - C_GUB_M_SOR

```python
self.vars[0]  = [[3.5, 4.5, 0.5], 4.0]                 # 범위갯수: 3   # 목표 수익률 (%)
self.vars[1]  = [[-3.5, -2.5, 0.5], -3.0]              # 범위갯수: 3   # 손절 수익률 (%)
self.vars[2]  = [[2.5, 3.5, 0.5], 3.0]                 # 범위갯수: 3   # 고점 대비 하락폭 (%)
self.vars[3]  = [[105, 135, 15], 120]                  # 범위갯수: 3   # 최대 보유시간 (분)
self.vars[4]  = [[0.965, 0.975, 0.005], 0.97]          # 범위갯수: 3   # 고가 대비 비율
self.vars[5]  = [[0.3, 0.7, 0.2], 0.5]                 # 범위갯수: 3   # 고가 하락 시 수익률 (%)
self.vars[6]  = [[0.8, 1.2, 0.2], 1.0]                 # 범위갯수: 3   # 음봉 전환 시 수익률 (%)
self.vars[7]  = [[95, 105, 5], 100]                    # 범위갯수: 3   # 체결강도 기준
self.vars[8]  = [[0.5, 0.7, 0.1], 0.6]                 # 범위갯수: 3   # 거래대금 비율
self.vars[9]  = [[1800, 2200, 200], 2000]              # 범위갯수: 3   # 시가총액 기준 (억원) - 소형
self.vars[10] = [[1.2, 1.8, 0.3], 1.5]                 # 범위갯수: 3   # 수익률 하한 (%) - 소형
self.vars[11] = [[3.5, 4.5, 0.5], 4.0]                 # 범위갯수: 3   # 수익률 상한 (%) - 소형
self.vars[12] = [[1.6, 2.0, 0.2], 1.8]                 # 범위갯수: 3   # 매도량 비율 - 소형
self.vars[13] = [[3, 7, 2], 5]                         # 범위갯수: 3   # 등락율각도 기준 (도) - 소형
self.vars[14] = [[4500, 5500, 500], 5000]              # 범위갯수: 3   # 시가총액 기준 (억원) - 중형
self.vars[15] = [[1.2, 1.8, 0.3], 1.5]                 # 범위갯수: 3   # 수익률 하한 (%) - 중형
self.vars[16] = [[3.5, 4.5, 0.5], 4.0]                 # 범위갯수: 3   # 수익률 상한 (%) - 중형
self.vars[17] = [[1.4, 1.8, 0.2], 1.6]                 # 범위갯수: 3   # 매도량 비율 - 중형
self.vars[18] = [[4, 8, 2], 6]                         # 범위갯수: 3   # 등락율각도 기준 (도) - 중형
self.vars[19] = [[1.2, 1.8, 0.3], 1.5]                 # 범위갯수: 3   # 수익률 하한 (%) - 대형
self.vars[20] = [[3.5, 4.5, 0.5], 4.0]                 # 범위갯수: 3   # 수익률 상한 (%) - 대형
self.vars[21] = [[1.3, 1.7, 0.2], 1.5]                 # 범위갯수: 3   # 매도량 비율 - 대형
self.vars[22] = [[5, 9, 2], 7]                         # 범위갯수: 3   # 등락율각도 기준 (도) - 대형

# 총 경우의 수: 3^23 = 94,143,178,827 (GA 최적화 필수)
```

### 매수·매도 통합 최적화 범위 - C_GUB_M_OR

```python
# 매수 핵심 변수 (6개)
self.vars[0]  = [[1500, 2500, 500], 2000]              # 범위갯수: 3   # 현재가 하한 (원)
self.vars[1]  = [[45000, 55000, 5000], 50000]          # 범위갯수: 3   # 현재가 상한 (원)
self.vars[2]  = [[1.5, 2.5, 0.5], 2.0]                 # 범위갯수: 3   # 갭상승율 하한 (%)
self.vars[3]  = [[115, 125, 5], 120]                   # 범위갯수: 3   # 체결강도 하한
self.vars[4]  = [[90, 110, 10], 100]                   # 범위갯수: 3   # 전일비 하한 (%)
self.vars[5]  = [[1800, 2200, 200], 2000]              # 범위갯수: 3   # 시가총액 기준1 (억원)

# 매도 핵심 변수 (5개)
self.vars[6]  = [[3.5, 4.5, 0.5], 4.0]                 # 범위갯수: 3   # 목표 수익률 (%)
self.vars[7]  = [[-3.5, -2.5, 0.5], -3.0]              # 범위갯수: 3   # 손절 수익률 (%)
self.vars[8]  = [[95, 105, 5], 100]                    # 범위갯수: 3   # 체결강도 기준
self.vars[9]  = [[105, 135, 15], 120]                  # 범위갯수: 3   # 최대 보유시간 (분)
self.vars[10] = [[0.965, 0.975, 0.005], 0.97]          # 범위갯수: 3   # 고가 대비 비율

# 총 경우의 수: 3^11 = 177,147
```

### 유전 알고리즘 범위 - C_GUB_M_GAR

```python
# 매수 핵심 변수 (6개)
self.vars[0]  = [[1500, 2000, 2500], 2000]             # 현재가 하한 (원)
self.vars[1]  = [[45000, 50000, 55000], 50000]         # 현재가 상한 (원)
self.vars[2]  = [[1.5, 2.0, 2.5], 2.0]                 # 갭상승율 하한 (%)
self.vars[3]  = [[115, 120, 125], 120]                 # 체결강도 하한
self.vars[4]  = [[90, 100, 110], 100]                  # 전일비 하한 (%)
self.vars[5]  = [[1800, 2000, 2200], 2000]             # 시가총액 기준1 (억원)

# 매도 핵심 변수 (5개)
self.vars[6]  = [[3.5, 4.0, 4.5], 4.0]                 # 목표 수익률 (%)
self.vars[7]  = [[-3.5, -3.0, -2.5], -3.0]             # 손절 수익률 (%)
self.vars[8]  = [[95, 100, 105], 100]                  # 체결강도 기준
self.vars[9]  = [[105, 120, 135], 120]                 # 최대 보유시간 (분)
self.vars[10] = [[0.965, 0.97, 0.975], 0.97]           # 고가 대비 비율

# 세대 수 × 개체 수: 예) 20세대 × 50개체 = 1,000회
```

## 조건식 개선 방향 연구

### 1. 추가 지표 활용 연구

**우선순위: 높음**
- **TA-Lib 지표 조합 확대**
  - ADX (추세 강도) + DMI (방향성) 조합
  - Ichimoku Cloud (일목균형표) 활용
  - 구현 난이도: 중

- **변동성 지표 추가**
  - ATR (Average True Range) 기반 손절폭 설정
  - Bollinger Band Width로 변동성 확대 구간 포착
  - 구현 난이도: 낮음

### 2. 시간대별 전략 세분화 연구

**우선순위: 높음**
- **장 시간대별 분봉 전략 차등화**
  - 09:00-10:00: 5분봉 전략
  - 10:00-14:00: 10분봉 전략
  - 14:00-15:00: 3분봉 전략
  - 구현 난이도: 중

### 3. 매도 조건 고도화 연구

**우선순위: 높음**
- **TA-Lib 매도 신호 강화**
  - MACD 데드크로스 + RSI 과매수 조합
  - Stochastic %K/%D 하향 교차 시 매도
  - 구현 난이도: 낮음

- **이동평균선 이탈 기반 매도**
  - 5MA 하향 돌파 시 경고
  - 20MA 하향 돌파 시 청산
  - 구현 난이도: 낮음

### 4. 리스크 관리 강화 연구

**우선순위: 높음**
- **다중 시간프레임 분석**
  - 1분봉 + 5분봉 + 10분봉 동시 확인
  - 상위 시간프레임 추세와 부합 시만 진입
  - 구현 난이도: 중

- **거래량 프로파일 분석**
  - 가격대별 거래량 분포 확인
  - 고거래량 구간 지지/저항 활용
  - 구현 난이도: 높음

### 5. 최적화 전략 개선 연구

**우선순위: 중간**
- **TA-Lib 파라미터 최적화**
  - RSI 기간: 14 → 10~20 범위 탐색
  - MACD (12,26,9) → (8,17,9) 등 변형 테스트
  - 구현 난이도: 중

- **기계학습 기반 지표 가중치 최적화**
  - 각 TA-Lib 지표의 예측력 평가
  - 가중치 동적 조정
  - 구현 난이도: 높음

### 구현 우선순위

1. **1단계 (즉시 구현)**:
   - 변동성 지표 추가 (ATR, BB Width)
   - TA-Lib 매도 신호 강화
   - 이동평균선 이탈 기반 매도

2. **2단계 (1개월 내)**:
   - TA-Lib 지표 조합 확대 (ADX, DMI, Ichimoku)
   - 시간대별 분봉 전략 차등화
   - 다중 시간프레임 분석

3. **3단계 (3개월 내)**:
   - TA-Lib 파라미터 최적화
   - 거래량 프로파일 분석
   - 기계학습 기반 최적화

